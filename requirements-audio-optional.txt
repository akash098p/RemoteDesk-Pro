# Optional audio dependencies.
# These may fail on Windows x86 / Python 3.14 because wheel support is limited.
# Install them only on a compatible environment if you want live audio sharing.
# Keep them separate from requirements.txt so the main app can install cleanly.

numpy==2.3.1
PyAudio==0.2.14
sounddevice==0.5.2
